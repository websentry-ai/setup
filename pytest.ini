[pytest]
# tests/ mirrors the source tree and is a package: several tools ship test files
# of the same basename (four test_identity.py, two test_setup.py), which only
# resolve to distinct modules when their directories are packages.
testpaths = tests binary/tests claude-code/hooks
norecursedirs = .git node_modules dist build *.egg-info
