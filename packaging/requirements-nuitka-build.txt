# Hash-pinned Nuitka build toolchain (WEB-4804, EDR bake-off vs PyInstaller).
# Nuitka 2.8.10 is the LAST Apache-2.0 release — 4.0+ relicensed to AGPLv3
# (with a commercial tier we must not use). Do not bump past 2.8.x without a
# license review.
# Nuitka ships as an sdist only; setuptools is pinned here (same hash as
# requirements-build.txt) and the install uses --no-build-isolation so the
# sdist builds with this pinned setuptools instead of an unpinned download.
# Regenerate: pip download --no-deps --no-binary :all: nuitka==<ver> && pip hash *.tar.gz
# Install:    pip install --require-hashes --no-deps --no-build-isolation -r requirements-nuitka-build.txt
nuitka==2.8.10 --hash=sha256:03e4d0756d8a11cb2627da3a2d9b518c802d031bf4f2c629e0a7b8c773497452
# Optional Nuitka speedup (pure-python wheel, MIT) — without it Nuitka warns
# about a "very slow fallback for ordered sets" and compile time doubles.
ordered-set==4.1.0 --hash=sha256:046e1132c71fcf3330438a539928932caf51ddbc582496833e23de611de14562
setuptools==82.0.1 --hash=sha256:a59e362652f08dcd477c78bb6e7bd9d80a7995bc73ce773050228a348ce2e5bb
