# Codex CLI Hooks for Unbound Gateway
Run the command to setup Codex CLI Hooks with Unbound:

```bash
python3 -c "$(curl -s https://raw.githubusercontent.com/websentry-ai/setup/refs/heads/main/codex/hooks/setup.py)" --domain gateway.getunbound.ai
```
